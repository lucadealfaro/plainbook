{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "NLBook",
            "type": "debugpy",
            "request": "launch",
            "module": "nlbook.main",
            "console": "integratedTerminal",
            "jinja": true,
            "justMyCode": true,
            "cwd": "${workspaceFolder}" // Ensure this is the root project folder
        }
    ]
}